# RemediationPlan

`RemediationPlan` describes the intended fix journey before execution.

It includes the fix preview, rollback plan, risk level, approval requirement, current status, user-facing message, next actions, timeline events, and external references.

Boundary rule:

`RemediationPlan` can explain and preview a fix. Execution still goes through AI LogFixer approval and safety checks.
